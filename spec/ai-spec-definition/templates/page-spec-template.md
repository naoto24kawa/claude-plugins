---
type: page
title: [PageName] ページ仕様書
version: 1.0.0
updated: YYYY-MM-DD
tags: []
related: []
dependencies: []
---

# [PageName] ページ仕様書

## 概要

**目的**: [このページの役割と目的を簡潔に記述]

**対象ユーザー**: [一般ユーザー / 管理者 / ゲスト]

**アクセス頻度**: [高 / 中 / 低]

---

## URL

### パス

```
/[path]/[subpath]
```

### パラメータ

#### パスパラメータ

| パラメータ名 | 型 | 説明 | 例 |
|------------|---|------|---|
| [param] | [type] | [説明] | [example] |

#### クエリパラメータ

| パラメータ名 | 型 | 必須 | デフォルト値 | 説明 |
|------------|---|------|------------|------|
| [param] | [type] | ✓ | - | [説明] |
| [param] | [type] | - | [value] | [説明] |

### URL例

```
/[path]/123?filter=active&page=1
```

---

## 権限要件

**必要な認証**: [必須 / 不要]

**必要な権限**: [admin / user / なし]

**権限がない場合の動作**: [ログインページへリダイレクト / 403エラー表示]

---

## 画面項目

### 主要な画面要素

| 要素名 | 種類 | 配置位置 | 説明 | 必須 |
|-------|------|---------|------|------|
| Header | 共通コンポーネント | 最上部 | サイト共通ヘッダー | ✓ |
| Breadcrumb | ナビゲーション | Header下 | パンくずリスト（例: Home > [PageName]） | ✓ |
| [Component1] | [種類] | メインエリア上部 | [説明] | ✓ |
| [Component2] | [種類] | メインエリア中部 | [説明] | - |
| Footer | 共通コンポーネント | 最下部 | サイト共通フッター | ✓ |

### 画面要素の階層構造

**ページ全体:**
- Header（共通）
- Breadcrumb
- Main Content Area
  - [Component1]
  - [Component2]
  - ...
- Footer（共通）

### レスポンシブ対応

**ブレークポイント**:
- モバイル: `< 640px`
- タブレット: `640px - 1024px`
- デスクトップ: `> 1024px`

**レイアウト変更**:
- モバイル時: [画面要素の配置変更や表示/非表示の切り替えを記述]
- タブレット時: [画面要素の配置変更を記述]

---

## 使用コンポーネント一覧

| コンポーネント名 | 用途 | Props |
|----------------|------|-------|
| [Component] | [用途] | [主要なProps] |

---

## データ取得

### 初回レンダリング時

**取得タイミング**: [Server-Side / Client-Side / Static Generation]

**データソース**:
- API: `GET /api/v1/[resource]`
- キャッシュ: [有効 / 無効]

**取得データ**: `[PageData]`
- `[field]`: [type] - [説明]

### データ更新タイミング

- [ ] ページマウント時
- [ ] パラメータ変更時
- [ ] ユーザーアクション後
- [ ] 定期的なポーリング（間隔: [interval]）

---

## ユーザーアクション一覧

### アクション1: [アクション名]

**トリガー**: [ボタンクリック / フォーム送信 / その他]

**処理内容**:
1. [ステップ1]
2. [ステップ2]
3. [ステップ3]

**API呼び出し**:
- エンドポイント: `POST /api/v1/[resource]`
- リクエストボディ: `{ [field]: [value] }`

**成功時の動作**:
- [ ] ページ遷移
- [ ] データ再取得
- [ ] 成功メッセージ表示
- [ ] モーダルを閉じる

**失敗時の動作**:
- [ ] エラーメッセージ表示
- [ ] フォームにエラーハイライト
- [ ] リトライボタン表示

---

## フォーム仕様（フォームがある場合）

### フォームフィールド一覧

| フィールド名 | 型 | 必須 | デフォルト値 | 説明 |
|------------|---|------|------------|------|
| [field] | [type] | ✓ | - | [説明] |

### バリデーションルール

#### フロントエンドバリデーション

| フィールド | ルール | タイミング | エラーメッセージ |
|-----------|--------|-----------|----------------|
| [field] | [rule] | onChange | [message] |
| [field] | [rule] | onBlur | [message] |

**実装方法**: [React Hook Form / Formik / カスタム実装]

#### サーバーサイドバリデーション

**API**: `POST /api/v1/[resource]`

**バリデーションエラーレスポンス**: `ValidationErrorResponse`
- `error.code`: "VALIDATION_ERROR"
- `error.details[]`: フィールドごとのエラー詳細
  - `field`: エラーフィールド名
  - `message`: エラーメッセージ

**エラー表示方法**:
- フィールド下にエラーメッセージ表示
- フィールド枠線を赤に変更
- フォーム上部にサマリー表示

### 送信前の最終チェック

- [ ] すべての必須項目が入力されているか
- [ ] すべてのバリデーションが通過しているか
- [ ] 重複送信防止（ボタン無効化）

### 送信処理

**ローディング表示**: [ボタン内スピナー / オーバーレイ / プログレスバー]

**成功時**:
- [ ] 成功メッセージ表示
- [ ] [遷移先ページ]へ遷移
- [ ] フォームをリセット

**失敗時**:
- [ ] エラーメッセージ表示
- [ ] フォーム状態を維持
- [ ] リトライ可能

---

## ページ遷移

### 遷移元

| ページ | トリガー |
|-------|---------|
| [Page] | [Action] |

### 遷移先

| ページ | トリガー | 遷移方法 |
|-------|---------|---------|
| [Page] | [Action] | [Link / Redirect / Push] |

### 遷移時のデータ保持

**保持方法**: [URL パラメータ / Session Storage / Context]

**保持データ**: `[NavigationData]`
- `[field]`: [type] - [説明]

---

## ローディング状態

### 初回ロード時

**表示内容**: [スケルトンスクリーン / スピナー / プログレスバー]

**表示位置**: [全画面 / コンテンツエリア / 各コンポーネント]

### データ更新時

**表示内容**: [インラインスピナー / 薄いオーバーレイ]

**既存データの扱い**: [表示継続 / 非表示]

---

## エラー状態

### データ取得エラー

**表示内容**:
```
エラーメッセージ:
データの取得に失敗しました。

[リトライボタン]
```

**リトライ動作**: 同じAPIを再度呼び出す

### 404エラー（リソース未検出）

**表示内容**:
```
お探しのページが見つかりませんでした。

[トップページへ戻る]
```

### 403エラー（権限不足）

**表示内容**:
```
このページにアクセスする権限がありません。

[ログインページへ]
```

### ネットワークエラー

**表示内容**:
```
ネットワーク接続を確認してください。

[リトライボタン]
```

---

## 状態管理

### 使用する状態管理ライブラリ

**ライブラリ**: [Redux / Zustand / Context API / なし]

**管理する状態**: `[PageState]`
- `[state]`: [type] - [説明]

### グローバル状態 vs ローカル状態

| 状態 | スコープ | 理由 |
|-----|---------|------|
| [state] | グローバル | [理由] |
| [state] | ローカル | [理由] |

---

## SEO / メタデータ

### ページタイトル

```
[PageName] | [SiteName]
```

### メタディスクリプション

```
[ページの説明文（120-160文字）]
```

### OGP設定

```html
<meta property="og:title" content="[Title]" />
<meta property="og:description" content="[Description]" />
<meta property="og:image" content="[ImageURL]" />
<meta property="og:url" content="[PageURL]" />
```

### 構造化データ（Schema.org）

```json
{
  "@context": "https://schema.org",
  "@type": "[Type]",
  "name": "[Name]"
}
```

---

## パフォーマンス要件

### 初回表示時間

**目標**: [LCP < 2.5秒]

**最適化手法**:
- [ ] コード分割（Code Splitting）
- [ ] 画像最適化（Next.js Image / lazy loading）
- [ ] CSS最適化（Critical CSS）
- [ ] サーバーサイドレンダリング（SSR）

### 相互作用までの時間

**目標**: [FID < 100ms]

**最適化手法**:
- [ ] JavaScriptバンドルサイズ削減
- [ ] 不要なライブラリの削除

---

## アクセシビリティ

### ページ構造

- [ ] 適切な見出し階層（h1 → h2 → h3）
- [ ] ランドマーク要素（header, main, nav, footer）
- [ ] スキップリンク

### キーボード操作

**主要なショートカット**:

| キー | 動作 |
|-----|------|
| Tab | フォーカス移動 |
| Enter | アクション実行 |
| Escape | モーダルを閉じる |

### スクリーンリーダー対応

- [ ] ページタイトルが読み上げられる
- [ ] フォームフィールドに適切なラベル
- [ ] エラーメッセージが読み上げられる
- [ ] ローディング状態が通知される（aria-live）

---

## AI実装時の注意事項

### データフロー
[コンポーネント間のデータの流れを図示または記述]

### エラーハンドリング戦略
- APIエラー: [ページレベルで捕捉 / コンポーネントレベルで捕捉]
- 境界エラー（Error Boundary）: [必要 / 不要]

### セキュリティ考慮事項
- [ ] XSS対策（ユーザー入力のサニタイゼーション）
- [ ] CSRF対策（トークン検証）
- [ ] 機密情報の表示制御（マスキング）

---

## 変更履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|---------|
| 1.0.0 | YYYY-MM-DD | 初版作成 |
