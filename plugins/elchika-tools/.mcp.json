{
  "mcpServers": {
    "elchika-tools": {
      "command": "sh",
      "args": [
        "-c",
        "cd \"${CLAUDE_PLUGIN_ROOT}/mcp-server\" && [ -d node_modules ] || npm install --include=dev --silent && npx tsx src/index.ts"
      ]
    }
  }
}
