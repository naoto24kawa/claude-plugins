# AI時代の開発プロセス設計書

## 1. 設計思想

### 1.1 目的

AIと人間が協働する開発プロセスにおいて、**仕様の作成・更新・修正を構造的に管理し、仕様と実装の双方向同期を実現する**仕組みを構築する。

### 1.2 基本原則

| 原則 | 説明 |
|------|------|
| **GitHubを開発のSSoT（Single Source of Truth）とする** | 要望管理以外のすべての開発情報はGitHubに集約する |
| **仕様は二層構造で管理する** | フロー型（Issue）とストック型（docs/specs/*.md）を併用する |
| **全職種が仕様に関われるようにする** | 非エンジニアはSlack/別UIから参加し、GitHubを直接操作する必要はない |
| **AIのインプット品質を仕組みで担保する** | GitHub Actionsによる品質ゲートで、仕様記述の精度を自動チェックする |
| **判断は常に人間が持つ** | AIは提案・検出・生成を行い、人間が承認する |

### 1.3 言語使い分けルール（日本語/英語）

AIの処理精度とチームの運用しやすさを両立するため、記述場所に応じて日本語と英語を使い分ける。

| 要素 | 言語 | 理由 |
|------|------|------|
| **frontmatter フィールド名** | 英語 | YAMLの慣例、ツール互換性 |
| **frontmatter `area` 値** | 英語（kebab-case） | フィルタリング精度、表記揺れ回避 |
| **frontmatter `tags` 値** | 英語（kebab-case） | 同上 |
| **frontmatter `title` 値** | 日本語 | 人間が読むタイトル |
| **ファイル名** | 英語（kebab-case） | URLエンコード問題の回避、Starlightパスの安定性 |
| **本文** | 日本語 | 全職種が読む |
| **GitHub ラベル** | 英語 | 表記揺れ回避、ツール互換性 |
| **コミットメッセージ type/scope** | 英語 | commitlint互換、Conventional Commits準拠 |
| **コミットメッセージ subject/body** | 日本語 | 判断理由を自然に書ける |
| **Issue / PR description** | 日本語 | 全職種が読む |

### 1.4 仕様の定義

本プロセスにおいて「仕様」は2種類に分類される。

**フロー型仕様（GitHub Issue）**

- 1 Issue = 1タスク（実装可能な粒度）
- ライフサイクルを持つ（open → 実装中 → close）
- 「**何を変えるか**」の契約書
- closeされた時点で契約が履行されたことを意味する

**ストック型仕様（docs/specs/*.md）**

- システムの現在の姿を記述する永続ドキュメント
- 「**今どうなっているか**」の記述
- AIがコンテキストとして直接参照できる
- 人間向けにはStarlight（Astro）でビルドしたWebサイトとして閲覧可能
- PRに含めて更新し、mainブランチのHEADと常に一致する

ストック型仕様の構造・フォーマット・人間向けビューの詳細は「docs/specs 仕様ドキュメント設計書」を参照。

### 1.5 関連ドキュメント

本設計書は以下のドキュメントと組み合わせて運用する。

| ドキュメント | 内容 | 対象読者 |
|-------------|------|---------|
| **本書（開発プロセス設計書）** | 全体フロー、ツール構成、品質ゲート、運用ステップ | 全員 |
| **docs/specs 仕様ドキュメント設計書** | ストック型仕様の構造、frontmatter、テンプレート、Starlight | エンジニア、AI |
| **CLAUDE.md 指示書** | Claude Codeへの具体的な指示（上記2つの内容を統合） | AI（Claude Code） |

---

## 2. ツール構成と役割分担

### 2.1 全体アーキテクチャ

```
[Slack]          [Notion]              [GitHub]                    [Starlight]
  │                │                      │                            │
  │  起票UI        │  要望管理             │  開発管理                    │  人間向け
  │                │  調査・議論           │                            │  仕様閲覧
  ▼                ▼                      │                            │
非エンジニアが     要望の判断              │                            │
自然言語で投稿  → （承認 / 却下）         │                            │
                   │                      │                            │
                   │ 承認時                │                            │
                   ▼                      ▼                            │
              AI が GitHub Issue を自動生成                              │
                                          │                            │
                                GitHub Issue（フロー型仕様）             │
                                          │                            │
                                GitHub Projects（バックログ管理）        │
                                          │                            │
                                Claude Code が実装                      │
                                          │                            │
                                PR（コード + docs/specs 更新）          │
                                          │                            │
                                レビュー・承認・merge                    │
                                          │                            │
                                          ├─→ docs/specs が最新に同期   │
                                          └─→ Starlightサイト自動更新 ──┘
```

### 2.2 各ツールの責務

| ツール | 責務 | 利用者 |
|--------|------|--------|
| **Slack** | 要望の起票UI。非エンジニアの入口 | 全職種 |
| **Notion** | 要望管理・調査・議論・判断の記録 | 全職種 |
| **GitHub Issue** | 実装タスクの仕様（フロー型）。AIが読み取る契約書 | エンジニア、AI |
| **GitHub Projects** | バックログの俯瞰・優先度管理・ロードマップ | PdM/企画、エンジニア |
| **GitHub PR** | 仕様の履行報告。コード変更 + 仕様ドキュメント更新のセット | Claude Code（作成）、エンジニア（レビュー） |
| **Git Commit** | 個々の変更の記録 | Claude Code（生成） |
| **docs/specs/*.md** | ストック型仕様。システムの現在の姿 | AI（直接参照）、全員（Starlight経由） |
| **Starlight** | docs/specs/のMarkdownからビルドした人間向け仕様閲覧サイト | 全職種 |
| **Claude Code** | 実装、仕様ドキュメント更新、差分検出 | AI |

---

## 3. 要望管理フロー（Notion）

### 3.1 要望のライフサイクル

```
Slack起票 → Notion登録（要望受付）
              → 調査中 → 提案作成 → 判断待ち → 承認 or 却下
                                                  │
                                                  ▼ 承認時
                                             GitHub Issueを生成
```

### 3.2 Notionの役割

Notionは要望の管理・調査・議論・判断記録を担う。具体的なデータベース設計は実装フェーズで決定するが、以下の情報を管理する必要がある。

- 要望の内容と背景
- ステータス（要望受付 → 調査中 → 提案作成 → 判断待ち → 承認/却下）
- 起票元と起票者
- 判断理由（承認・却下の根拠）
- 承認後に生成されたGitHub Issueへのリンク

### 3.3 要望に紐づくタスク

要望には実装前の作業が発生する。これらはフラットなGitHub Issueとして管理し、`refs` で要望のNotionページまたは関連Issueへ紐付ける。

- 調査タスク（利用データ分析、競合調査等）
- 提案タスク（改善案の作成、プロトタイプ等）
- 実装タスク（承認後に生成）

```
Issue #11 [type:research] 応募ボタンの利用データ分析（refs notion-xxx）
Issue #12 [type:research] 競合の応募UI調査（refs notion-xxx）
Issue #13 [type:implementation] 応募ボタンをフローティングに変更（refs notion-xxx, refs #11, refs #12）
```

### 3.4 Notion → GitHub Issue 変換

要望が承認されたとき、AIがNotionの要望ページを元にGitHub Issueを自動生成する。変換時にAIは要望をIssue Templateに沿った実装可能な粒度に構造化する。具体的な変換ロジックは実装フェーズで決定する。

---

## 4. 開発フロー（GitHub）

### 4.1 Issue の設計

#### Issue Template

すべての実装タスクIssueは以下のテンプレートに従う。セクション見出しは固定、中身は自然言語で記述可能。

```markdown
## 概要
<!-- このタスクで何を実現するかを1-2文で -->

## 背景・目的
<!-- なぜこの変更が必要か。Notion要望ページへの参照を含む -->
refs notion-xxx

## 受け入れ条件
<!-- 検証可能な条件をリストで記述する -->
- [ ] 条件1
- [ ] 条件2
- [ ] 条件3

## 影響範囲
<!-- 変更が影響するモジュール、画面、APIを記述する -->

## 関連ファイル
<!-- 変更対象となるファイルパスや関連するドキュメント -->
- docs/specs/xxx.md

## 備考
<!-- 技術的制約、注意事項、参考情報 -->
```

#### ラベル体系

ラベルはすべて英語で記述する（表記揺れ回避、ツール互換性のため）。

| ラベルカテゴリ | 値 | 説明 |
|----------------|------|------|
| **type** | `type:implementation` `type:research` `type:bug` `type:improvement` `type:documentation` | タスクの種別 |
| **priority** | `priority:high` `priority:medium` `priority:low` | 優先度 |
| **status** | `status:ready` `status:in-progress` `status:in-review` | 進行状態 |
| **area** | `area:job-management` `area:application` `area:auth` 等 | 機能領域 |

### 4.2 コミットメッセージ規約

Conventional Commitsをベースとし、以下のルールを厳守する。

#### フォーマット

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### type一覧

| type | 用途 |
|------|------|
| `feat` | 新機能追加 |
| `fix` | バグ修正 |
| `docs` | ドキュメントのみの変更 |
| `refactor` | リファクタリング |
| `test` | テストの追加・修正 |
| `chore` | ビルド、CI等の変更 |

#### 必須ルール

1. **scope は機能領域を英語（kebab-case）で記述する**（例: `feat(job-management):` `fix(application-form):`）
2. **subject は日本語で簡潔に記述する**
3. **body には「なぜこの実装方法を選んだか」を日本語で含める**
4. **footer には必ず関連Issueへの参照を含める**（`refs #234` または `closes #234`）

#### 例

```
feat(job-management): 公開ステータスの一括変更APIを追加

- PATCH /api/jobs/bulk-status エンドポイント新設
- 権限チェックをミドルウェアで実施
- パフォーマンス観点から100件上限のバリデーションを追加
  （Issueに上限の記載はなかったが、DBクエリの負荷を考慮して設定）

refs #234
```

### 4.3 PR の設計

#### PR Description Template

```markdown
## 対応Issue
closes #XXX

## 変更概要
<!-- 何をどう変更したかの要約 -->

## 受け入れ条件の充足
<!-- Issueの受け入れ条件に対応するチェックリスト -->
- [x] 条件1
- [x] 条件2
- [ ] 条件3（理由: ○○のため次Issueに切り出し → #YYY）

## 仕様からの変更点
<!-- 実装中に判明した仕様の曖昧さ、追加判断、変更があればここに記述 -->
<!-- なければ「なし」と記述 -->

## 仕様ドキュメント更新
<!-- docs/specs/ の変更有無と変更内容の要約 -->
- [ ] docs/specs/xxx.md 更新あり
- [ ] 仕様更新の必要なし（理由: ○○）
```

#### PRに含まれるもの

1. **コードの変更**
2. **docs/specs/*.md の更新**（仕様に影響する変更がある場合は必須）
3. **テスト**

この3つが常にセットでPRに入ることで、仕様とコードの同期が構造的に保証される。

### 4.4 GitHub Projects の活用

#### 役割

GitHub Projectsは**バックログの俯瞰と意思決定のUI**として使う。データの実体はIssue側にあり、Projectsはビューレイヤーに徹する。

**注意:** Projectsのカスタムフィールドに保存された情報（優先度、ステータス等）はIssue本体には保存されない。AIに渡したい情報は必ずIssue本文またはラベルに記載すること。

#### ビュー構成

対象者ごとに異なるビューを提供する。

- PdM/企画向け：ステータス別カンバンビュー
- エンジニア向け：優先度×見積もりのテーブルビュー
- 経営/全員向け：リリース単位のロードマップビュー

### 4.5 Issue 間の関連付け

Issue間の関係はフラットに `refs #XX` で紐付ける。親子関係（Sub-issues）は使用しない。

**理由:**

- AIのデータ参照コストを最小化する（1ホップで完結）
- 運用の複雑性を抑える
- GitHub Sub-issuesの機能成熟度への依存を避ける

**ルール:**

- 各Issueは本文内に作業に必要な情報を完結して記述する
- 関連Issueは `refs #XX` で参照するが、参照先を読まなくても作業可能な粒度を保つ
- Projectsのフィルタ・グルーピングで関連Issueの俯瞰は可能

---

## 5. 仕様と実装の同期メカニズム

### 5.1 同期の基本ルール

**PRにはコード変更と仕様変更が常にセットで入る。**

これにより、docs/specs/ のMarkdownは常にmainブランチのHEADと一致し、Gitの履歴でいつ・なぜ変わったかも追える。

### 5.2 変更理由の追跡チェーン

任意の仕様変更から、その根拠となる要望まで一本の線で辿れる。

```
docs/specs/ の特定の記述
  ↑ git log
    → commit（何を・どう変えたか、なぜその実装方法か）
      ↑ commit footer の refs/closes
        → PR（受け入れ条件の充足状況、仕様からの変更点）
          ↑ PR の closes #XX
            → Issue（何を変えるか、受け入れ条件）
              ↑ Issue 本文の refs notion-xxx
                → Notion 要望ページ（なぜやるか、判断理由、調査結果）
```

### 5.3 仕様ドキュメントの記述方針

- 仕様ドキュメントには「**何がどうなっているか**」を記述する
- 「**なぜそうなっているか**」はPR description、Issue、decisions/ に残す
- この分離により、仕様は現状の事実として信頼でき、変更理由は追跡チェーンで辿れる

---

## 6. GitHub Actionsによる品質ゲート

### 6.1 基本方針

- **ブロックはしない、指摘する**（bot コメントで警告）
- ただし**トレーサビリティの土台**となる項目はCIで落とす
- 最初は警告ベースで運用し、チーム習熟後に段階的に必須化する

### 6.2 Issue 作成時

| チェック項目 | 強度 |
|-------------|------|
| テンプレート必須セクションの充足 | 警告 |
| 受け入れ条件の検証可能性（曖昧な条件に具体化を促す） | 警告 |
| type / area ラベルの自動付与 | 自動実行 |
| 関連Issue/Notion リンクの有無 | 警告 |

### 6.3 PR 作成時

| チェック項目 | 強度 |
|-------------|------|
| `closes #XX` の記載 | **CI失敗**（必須） |
| 受け入れ条件チェックリストの存在 | 警告 |
| 仕様との差分セクションの記載 | 警告 |
| docs/specs 更新有無の明示 | 警告 |
| コミットメッセージの Conventional Commits 準拠 + Issue参照 | **CI失敗**（必須） |
| 紐づくIssueのステータス確認 | 警告 |

### 6.3.1 docs/specs ドキュメント品質チェック（PR時）

docs/specs/ 配下のファイルに変更が含まれるPRに対して追加チェックを実行する。チェック対象のfrontmatterフィールド定義は「docs/specs 仕様ドキュメント設計書 第4章」に準拠する。

| チェック項目 | 強度 |
|-------------|------|
| frontmatterスキーマ準拠 | **CI失敗**（必須） |
| 必須フィールド（type, title, area, tags, doc_status, created, updated）の充足 | **CI失敗**（必須） |
| `updated` フィールドの更新確認 | 警告 |
| deprecatedファイルの更新検出 | 警告 |
| テンプレート必須セクション確認（type別） | 警告 |
| Mermaid構文チェック | 警告 |
| `related` のリンク切れ検出 | 警告 |
| 新規API追加時のドキュメント存在確認 | 警告 |

### 6.4 PR マージ時

| アクション | 説明 |
|-----------|------|
| 仕様変更サマリー自動生成 | docs/specs の diff を要約し、レビュアーに情報提供 |
| Notion への完了通知 | 元の要望ページに完了を通知 |
| Slack への完了通知 | チーム全体に共有 |
| Issue 自動クローズ確認 | `closes #XX` の動作確認 |
| Starlightサイト再ビルド | 人間向け仕様サイトの自動更新 |

### 6.5 定期実行（週次）

| アクション | 説明 |
|-----------|------|
| 仕様-コード乖離検出 | docs/specs の記述とコードの実態を比較 |
| `related_tables` / `related_apis` の存在確認 | frontmatterの参照先がコード上に実在するか |
| Mermaid図とコードの整合性確認 | 状態遷移図と実装の遷移ロジックの乖離検出 |
| `doc_status: draft` 放置検出 | 長期間draftのまま放置されたファイルの検出 |
| `related` リンク切れ全件検出 | 全ファイルのリンク切れ一括チェック |
| 放置Issue検出 | 長期間更新なしのIssueの検出 |
| 仕様カバレッジレポート | 仕様が未記述の機能領域の可視化 |

### 6.6 CI必須項目（初期段階）

初期段階からCIで失敗させる項目は以下の3つのみ。これらはトレーサビリティと仕様品質の土台であり、ここが崩れると仕組み全体が機能しなくなる。

1. **PR に `closes #XX` が含まれていること**
2. **コミットメッセージが Conventional Commits に準拠し、Issue 参照を含むこと**
3. **docs/specs/ のfrontmatterがスキーマに準拠していること**（変更があるファイルのみ対象）

---

## 7. Claude Code への指示（CLAUDE.md）

Claude Code への具体的な指示は「CLAUDE.md 指示書」に統合して記載する。本設計書と「docs/specs 仕様ドキュメント設計書」の両方のルールを1ファイルにまとめ、Claude Codeが参照すべき情報を一元化する。

CLAUDE.md に記載する内容の概要：

- Issue の読み取りルール
- コミットメッセージ規約
- PR 作成ルール
- docs/specs/ の参照・新規作成・更新ルール（命名規則、Mermaid図の記述ルールを含む）
- 仕様ドキュメントの記述方針（「何がどうなっているか」を書き、「なぜ」はPR/Issueに残す）

詳細は「CLAUDE.md 指示書」を参照。

---

## 8. 運用開始ステップ

本設計書と「docs/specs 仕様ドキュメント設計書」の運用開始を以下のPhase順で進める。各Phaseは前のPhaseの完了を前提とする。

### Phase 1: 土台構築

開発プロセスの基盤とdocs/specsの基盤を同時に構築する。Issue/PR Template、コミットメッセージ規約、docs/specsのディレクトリ構成・スキーマ・テンプレート、CLAUDE.md指示書を整備する。

### Phase 2: 初期ドキュメント生成

docs/specsの初期コンテンツを作成する。既存コードベースから主要な機能・概念を洗い出し、`doc_status: draft` でドラフトを作成。レビュー後に `stable` に昇格する。生成方法は実装フェーズで決定する。

### Phase 3: 品質ゲート導入

GitHub Actionsによる自動チェックを導入する。まずCI必須の3項目（`closes #XX`、コミットメッセージlint、frontmatterバリデーション）を導入し、その後警告ベースのチェックを段階的に追加する。

### Phase 4: 人間向けビュー構築

Starlightによる仕様閲覧サイトを構築する。merge時の自動ビルド・デプロイを設定する。設定は「docs/specs 仕様ドキュメント設計書 第7章」に準拠する。

### Phase 5: 要望管理連携

Notion/Slackとの連携を構築する。Slack起票フロー、Notion要望管理データベース、Notion→GitHub Issue自動生成、マージ時の通知を整備する。

### Phase 6: 定期チェック

週次の仕様-コード乖離検出、放置Issue検出、仕様カバレッジレポートを導入する。警告ベースのチェックのうち成熟した項目をCI必須に昇格する。

---

## 9. リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| **運用の形骸化**（テンプレートが雑に埋められる） | 仕様の品質低下、トレーサビリティの破綻 | 品質ゲートによる自動検出 + 段階的な必須化 |
| **Issue の肥大化**（大量のIssueでノイズが増える） | AIの検索精度低下、人間の俯瞰性低下 | ラベル体系の厳格化、Projectsのビュー活用、完了Issueのアーカイブ |
| **Notion-GitHub間の情報断絶** | 要望の背景が実装時に参照されない | `refs notion-xxx` の記載を品質ゲートで確認、Notion側にもGitHub Issueリンクを記録 |
| **docs/specs の更新漏れ** | ストック型仕様が古くなる | PR時のdocs/specs更新チェック、週次の乖離検出 |
| **AIが生成するコミットメッセージの品質** | 変更理由が追えなくなる | CLAUDE.md での指示 + コミットメッセージlintによる形式チェック |
| **docs/specsのfrontmatter不備** | バリデーション不通過、メタデータの信頼性低下 | CIでのスキーマバリデーション必須化 |
